/out/
/result
/result-*
.DS_Store

# release.sh writes its rewrite of package-no-glx.nix here; a killed run can leave one
# behind, and an untracked stray would confuse the worktree-clean guard.
/.package-no-glx.nix.release.*

